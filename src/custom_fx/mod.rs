//! Слой пользовательских эффектов вибрации (конструктор кастомных эффектов):
//! модель настроек одного эффекта (`model`), таблица доступных источников
//! телеметрии (`sources`), движок расчёта (`engine`) и хранение списка на
//! диске (`store`). UI и подключение к воркерам — в других модулях, поверх
//! этого контракта.

pub mod engine;
pub mod model;
// Таблица вытеснения "источник -> основной встроенный эффект" + сведение
// выходов встроенного и пользовательского движков по каналу мотора (см.
// overrides.rs) — используется всеми тремя воркерами (sim/wt_link/xp_link),
// теперь оба движка считаются каждый тик одновременно, а не как режимы.
pub mod overrides;
pub mod player;
pub mod sources;
pub mod store;
